<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.food.model.dao.UserDao">
	<resultMap type="User" id="UserMap">
		<result column="User_Id" property="UserId"/>
		<result column="User_Password" property="UserPassword"/>
		<result column="User_NickName" property="UserNickName"/>
		<result column="User_Cha" property="UserCha"/>
	</resultMap>

	<!-- <select id="selectAll" resultType="User">
		SELECT  User_Id, User_Pass, User_Name,
				User_Cha, 
		FROM user u, review r
		WHERE 이거 해야하나 보류
	</select> -->

	<insert id="insertUser" parameterType="User">
		INSERT INTO user
		VALUES (#{userId}, #{userPass}, #{userName}, #{userCha})
	</insert>
	
	<select id="selectOne" parameterType="String" resultMap="UserMap">
		SELECT User_Id, User_Password, User_NickName, User_Cha
		FROM user
		WHERE  User_Id = #{id}
	</select>
	
	
	
</mapper>